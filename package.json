{
  "name": "brasil-lottery-api",
  "version": "0.0.0",
  "description": "Static API for Federal lottery results from Caixa Brasil",
  "main": "scripts/process-federal.js",
  "scripts": {
    "build": "node scripts/process-federal.js",
    "dev": "npm run build && npx http-server dist -p 3000"
  },
  "keywords": ["lottery", "federal", "api", "caixa", "static", "brasil", "loteria"],
  "dependencies": {
    "node-xlsx": "^0.23.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}